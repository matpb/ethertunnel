//! `etun` — the EtherTunnel binary. Dispatches to the relay or client crate.
//!
//! M0 placeholder: the full clap surface (serve / login / add / up / status /
//! service / doctor / admin) lands across M2–M8.

fn main() {
    println!(
        "etun {} — scaffold (see milestone plan)",
        env!("CARGO_PKG_VERSION")
    );
}
